import React from 'react';nimport ReactDOM from 'react-dom/client';nimport './index.css';nimport App from './App';nimport reportWebVitals from './reportWebVitals';nimport { Provider } from 'react-redux';nimport store from './store/store';nimport { BrowserRouter } from 'react-router-dom';nnconst root = ReactDOM.createRoot(n  document.getElementById('root') as HTMLElementn);nroot.render(n  <React.StrictMode>n    <BrowserRouter>n      <Provider store={store}>n        <App />n      </Provider>n    </BrowserRouter>n  </React.StrictMode>n);nn// If you want to start measuring performance in your app, pass a functionn// to log results (for example: reportWebVitals(console.log))n// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitalsnreportWebVitals();